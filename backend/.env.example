# Copy to .env and fill in values
# Never commit .env to git

DATABASE_URL=postgresql+psycopg://postgres:password@localhost:5432/geargap

BLIZZARD_CLIENT_ID=your_client_id
BLIZZARD_CLIENT_SECRET=your_client_secret

# Phase 2
ANTHROPIC_API_KEY=
