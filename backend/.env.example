# Copy to .env and fill in values
# Never commit .env to git

# Supabase PostgreSQL (Transaction Pooler, psycopg3 드라이버)
DATABASE_URL=postgresql+psycopg://postgres.PROJECT_REF:PASSWORD@aws-X-REGION.pooler.supabase.com:6543/postgres

BLIZZARD_CLIENT_ID=your_client_id
BLIZZARD_CLIENT_SECRET=your_client_secret

# Raidbots static data hash — rotate on each game client build
# How to get: https://www.raidbots.com/developers → Network tab → static/data/{hash}/...
RAIDBOTS_HASH=your_raidbots_hash

# Current WoW patch version — must match a row in patch_versions table
# Example: 12.0.5 (Midnight Season 1)
CURRENT_PATCH_VERSION=12.0.5

# Phase 2
ANTHROPIC_API_KEY=
