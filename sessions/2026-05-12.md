# Session — 2026-05-12

## 작업 목표

Obsidian Vault 문서 기반으로 Claude Code 하네스 구성 + 백엔드/프론트엔드 스캐폴딩

---

## 한 일

### 1. Obsidian Vault 문서 파악

경로: `C:\Users\Windows10\Documents\ObsidianVault\Projects\GearGap\`

| 문서 | 내용 |
|------|------|
| `GearGap_PRD.md` | PRD v0.5 — 기획, 페르소나, F1~F11 기능 |
| `GearGap_설계.md` | 기술 스택, 아키텍처 |
| `GearGap_ERD.md` | 10개 테이블 ERD |
| `GearGap_데이터 파이프라인 설계.md` | 3트랙 파이프라인 |
| `GearGap 인프라 구축 및 배포 계획서.md` | Vercel + Cloud Run + Supabase v1.2 |
| `Walking Skeleton 1주 실전 가이드.md` | Day 1~7 구현 가이드 |

---

### 2. `.claude` 하네스 구성

Reference: `first-fluke/fullstack-starter`, `first-fluke/oh-my-agent`

생성 파일:

```
CLAUDE.md                        ← 프로젝트 메인 (효율 공식, 아키텍처, 금지 규칙)
.claude/
├── settings.json                ← npm/uvicorn/pip 권한, effortLevel: high
└── rules/
    ├── design.md                ← GearGap 철학 (금지어, 투명성, 근거 패널)
    ├── backend.md               ← FastAPI 레이어 구조 / Blizzard API 패턴
    ├── frontend.md              ← React 컴포넌트 구조 / 환경변수
    ├── database.md              ← ERD 10테이블 / is_active vs is_latest 구분
    ├── infrastructure.md        ← Vercel + Cloud Run + Supabase / CORS
    ├── dev-workflow.md          ← 로컬 실행 명령어 / 브랜치 전략
    └── commit.md                ← 커밋 타입 / 스코프 / 예시
```

`.gitignore` 수정:
- `.claude/` 전체 무시 → `.claude/settings.local.json` 만 무시로 변경
- `*.md` 무시에 `!.claude/**/*.md` 예외 추가

---

### 3. 백엔드 스캐폴딩

Reference: `fastapi/full-stack-fastapi-template`, `first-fluke/fullstack-starter`

채택한 패턴:
- `app/` 서브패키지 구조 (fastapi 템플릿)
- SQLModel (Pydantic + SQLAlchemy 통합)
- `core/config.py` — pydantic-settings
- `api/deps.py` — `SessionDep` DI 패턴
- `api/v1/router.py` — 라우터 집합
- Alembic 마이그레이션
- `pyproject.toml` + `ruff` (fullstack-starter)
- `/health`, `/health/live` 헬스체크 (fullstack-starter)

생성 구조:

```
backend/
├── app/
│   ├── main.py                  # FastAPI app, CORS, lifespan
│   ├── api/
│   │   ├── deps.py              # SessionDep
│   │   └── v1/
│   │       ├── router.py
│   │       └── routes/
│   │           ├── characters.py
│   │           ├── efficiency.py
│   │           └── admin.py
│   ├── core/
│   │   ├── config.py            # pydantic-settings
│   │   └── db.py
│   ├── models/
│   │   ├── patch.py             # PATCH_VERSIONS
│   │   ├── character.py         # CHARACTERS, CHARACTER_EQUIPMENT, CHARACTER_STATS
│   │   ├── meta.py              # CLASSES, SPECS
│   │   ├── item.py              # ITEMS, CONTENTS, DROP_SOURCES
│   │   └── simulation.py        # SIMULATION_RESULTS
│   ├── schemas/
│   │   ├── character.py
│   │   └── efficiency.py        # EvidenceDetail, EfficiencyCandidate, WeightParams
│   ├── services/
│   │   ├── blizzard.py          # OAuth 토큰 캐싱 + realm slug 매핑
│   │   └── efficiency.py        # 효율 점수 공식 구현
│   └── repositories/
│       └── character_repo.py    # 10분 캐시 패턴
├── alembic/
│   ├── env.py
│   └── versions/
├── Dockerfile
├── .env.example
├── alembic.ini
└── pyproject.toml
```

#### 버그 수정 (스캐폴딩 직후 발견)

| 위치 | 버그 | 수정 |
|------|------|------|
| `app/core/db.py` | `SQLModel.text()` — 존재하지 않는 메서드 | `from sqlalchemy import text` + `text("SELECT 1")` |
| `app/models/item.py` | `Field(decimal_places=4, max_digits=6)` — SQLModel Field에 없는 파라미터 | `SAColumn(Numeric(precision=6, scale=4))` |
| `app/models/simulation.py` | 동일 | `SAColumn(Numeric(precision=10, scale=2))` |

---

### 4. 프론트엔드 현황 파악 + API 레이어 추가

#### 발견한 것

프론트엔드가 이미 상당히 구현되어 있었음:
- 화면 5개 완성 (Landing, Loading, Analysis, Recommendations, Error)
- 컴포넌트 (WeightControls, ComparisonTable, Prov, CharHeader 등) 완성
- CSS 디자인 시스템 완성
- 전체 `data/mock.ts` 목 데이터로만 동작 중 (백엔드 연결 없음)

#### 공식 불일치 발견

| | 공식 |
|---|---|
| 백엔드 `efficiency.py` | `(dps_gain × W_dps) ÷ (avg_clear_minutes × W_time) × (drop_rate × W_prob)` |
| 프론트 `utils/scores.ts` | 각 지표 0-1 정규화 후 가중 합산 |

→ API 연결 시 통일 필요

#### 추가한 파일

```
frontend/src/api/
├── client.ts     # api.getCharacter(), api.getEfficiency()
└── types.ts      # 백엔드 응답 타입 (ApiCharacter, ApiCandidate, ApiEvidence)
frontend/.env.local
frontend/.env.example
```

`.claude/rules/frontend.md` — 실제 구조(`screens/`, `api/`, CSS 토큰 방식 등) 반영

---

## 현재 상태

| 영역 | 상태 |
|------|------|
| `.claude` 하네스 | ✅ 완성 |
| 백엔드 스캐폴딩 | ✅ 완성 (버그 3개 수정) |
| 프론트 API 레이어 | ✅ `api/client.ts` 추가 |
| DB 연결 | ❌ Supabase 미설정, `.env` 미작성 |
| Alembic migration | ❌ 파일 없음 (`alembic revision` 미실행) |
| 백엔드 실행 | ❌ Python 미설치 / 의존성 미설치 |
| 프론트 API 연결 | ❌ 화면들 여전히 mock 데이터 사용 |
| Blizzard API 키 | ❌ 미등록 |

---

## 다음 작업 (Walking Skeleton Day 2~4 순서)

1. Python 설치 + `pip install -r requirements.txt`
2. Supabase 프로젝트 생성 + `backend/.env` 작성
3. `alembic revision --autogenerate -m "init"` + `alembic upgrade head`
4. 백엔드 로컬 실행 확인 (`uvicorn main:app --reload`)
5. `LandingScreen` → `LoadingScreen` 전환에 실제 `api.getCharacter()` 연결
6. 효율 점수 공식 프론트/백엔드 통일
